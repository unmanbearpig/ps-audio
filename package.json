{
  "private": true,
  "scripts": {
    "build": "pulp build -I src/Music --to dist/app.js",
    "watch": "pulp -w build -I src/Music --to dist/app.js",
    "watch-fast": "webpack --entry ./entry.js --output-filename dist/app.js --progress --watch"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.6",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  }
}
